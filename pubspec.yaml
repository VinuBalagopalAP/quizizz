name: centrric_assignment
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.11
  provider: ^6.0.3

  cupertino_icons: ^1.0.5
  animated_text_kit: ^4.2.2
  rive: ^0.9.1
  lottie: ^1.4.2
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
